# 📋 سامانه مدیریت ارتباطات و گردش کار کیا گستر

> **KiaGostar CRM** — سامانه جامع مدیریت داخلی سازمان

---

## 📑 فهرست مطالب

1. [نمای کلی پروژه](#۱-نمای-کلی-پروژه)
2. [ Stack فناوری ](#۲-stack-فناوری)
3. [ساختار پروژه](#۳-ساختار-پروژه)
4. [معماری نرم‌افزار](#۴-معماری-نرم‌افزار)
5. [مدل داده و موجودیت‌ها](#۵-مدل-داده-و-موجودیت‌ها)
6. [سیستم احراز هویت و مجوزها](#۶-سیستم-احراز-هویت-و-مجوزها)
7. [ نقش‌ها و داشبوردها ](#۷-نقش‌ها-و-داشبوردها)
8. [ ماژول‌های عملیاتی ](#۸-ماژول‌های-عملیاتی)
9. [ سیستم UI و کامپوننت‌ها ](#۹-سیستم-ui-و-کامپوننت‌ها)
10. [گردش‌کار و فرآیندها](#۱۰-گردش‌کار-و-فرآیندها)
11. [امنیت](#۱۱-امنیت)
12. [تنظیمات و اجرا](#۱۲-تنظیمات-و-اجرا)
13. [پیشنهادات بهبود UX](#۱۳-پیشنهادات-بهبود-ux)
14. [فهرست فایل‌ها](#۱۴-فهرست-فایل‌ها)

---

## ۱. نمای کلی پروژه

### معرفی

**کیا گستر CRM** یک سامانه مدیریت داخلی سازمان است که برای مدیریت جلسات، وظایف، گزارش‌ها، درخواست‌ها و فرآیندهای سازمانی طراحی شده است. این سامانه با رویکرد **DDD (Domain-Driven Design)** و معماری **Feature-Based** پیاده‌سازی شده و از سیستم کنترل دسترسی مبتنی بر نقش (RBAC) و قابلیت (Capability-Based) استفاده می‌کند.

### ویژگی‌های کلیدی

| ویژگی | توضیح |
|--------|--------|
| 🏢 **مدیریت سازمانی** | ساختار سازمانی، واحدها، سمت‌ها و کاربران |
| 📅 **مدیریت جلسات** | ایجاد، زمان‌بندی، برگزاری و صورتجلسه جلسات |
| ✅ **مدیریت وظایف** | ایجاد، محول‌سازی، پیگیری و گزارش‌دهی وظایف |
| 📊 **گزارش‌دهی** | گزارش‌های عملکردی، مالی و سازمانی |
| 📝 **درخواست‌ها** | ثبت و پیگیری درخواست‌های سازمانی |
| 📋 **مصوبات** | ثبت و پیگیری مصوبات جلسات |
| 📁 **مدیریت فایل** | بارگذاری و مدیریت مستندات |
| 🔔 **اعلان‌ها** | سیستم اعلان هوشمند |
| 🔍 **جستجوی سراسری** | جستجو در تمام بخش‌ها |
| 📊 **داشبورد هوشمند** | داشبورد شخصی‌سازی شده برای هر نقش |
| 🛡️ **امنیت پیشرفته** | کنترل دسترسی مبتنی بر نقش و قابلیت |
| 📱 **رابط کاربری واکنش‌گرا** | سازگار با دستگاه‌های مختلف |
| 🌐 **پشتیبانی RTL** | رابط کاربری راست‌به‌چپ |
| ♿ **دسترسی‌پذیری** | استانداردهای WCAG |

---

## ۲. Stack فناوری

### فرانت‌اند

| فناوری | نسخه | کاربرد |
|--------|------|--------|
| **React** | 19.x | کتابخانه اصلی UI |
| **TypeScript** | 6.x | زبان برنامه‌نویسی |
| **Vite** | 8.x | ابزار build و dev server |
| **React Compiler** | 1.x | بهینه‌سازی خودکار رندر |
| **ESLint** | 10.x | بررسی کیفیت کد |

### ابزارهای توسعه

| ابزار | کاربرد |
|-------|--------|
| **Babel** | ترنسپایل و بهینه‌سازی |
| **TypeScript ESLint** | قوانین linting اختصاصی |
| **Vite Plugin React** | پشتیبانی JSX در Vite |

### وابستگی‌ها

> این پروژه **بدون وابستگی خارجی UI** پیاده‌سازی شده و تمام کامپوننت‌ها از صفر نوشته شده‌اند.

- **react** / **react-dom** — تنها وابستگی اجرایی

---

## ۳. ساختار پروژه

```
KiaGostar_Crm/
├── public/                    # فایل‌های استاتیک
│   ├── favicon.svg
│   └── icons.svg
├── src/
│   ├── app/                   # لایه اپلیکیشن
│   │   ├── layout/            # لایوت اصلی (Sidebar, Header, etc.)
│   │   ├── navigation/        # پیکربندی ناوبری
│   │   ├── presentation/      # برچسب‌ها و متن‌ها
│   │   └── routes/            # مسیریابی
│   ├── assets/                # فونت‌ها و تصاویر
│   │   └── fonts/             # فونت Shabnam
│   ├── components/            # کامپوننت‌های مشترک
│   │   ├── common/            # Container, Pagination, DataTable
│   │   ├── dashboard/         # کامپوننت‌های داشبورد
│   │   ├── feedback/          # Loading, ErrorBoundary, AsyncStates
│   │   ├── ui/                # کتابخانه UI اختصاصی
│   │   │   ├── data-display/  # Table, DataGrid, StatusBadge, etc.
│   │   │   ├── forms/         # TextField, Select, DatePicker, etc.
│   │   │   ├── overlay/       # Modal, Drawer, Toast, Menu
│   │   │   └── ...            # Button, Card, Avatar, Badge, etc.
│   │   └── work/              # KanbanBoard, WorkPanel
│   ├── config/                # تنظیمات محیطی
│   ├── core/                  # هسته سیستم
│   │   ├── authorization/     # کنترل دسترسی
│   │   ├── permissions/       # مجوزها
│   │   └── utils/             # توابع کمکی
│   ├── demo/                  # داده‌های نمونه
│   │   ├── demoDataset.ts     # مجموعه داده اصلی
│   │   ├── demoDashboard.ts   # داده‌های داشبورد
│   │   ├── demoMeetings.ts    # داده‌های جلسات
│   │   ├── demoOrganization.ts# داده‌های سازمانی
│   │   ├── demoStore.ts       # فروشگاه داده ری‌اکتیو
│   │   ├── demoTypes.ts       # تایپ‌های داده نمونه
│   │   └── demoViews.ts       # ویوهای ارائه‌ای
│   ├── domain/                # لایه دامنه
│   │   ├── entities/          # موجودیت‌ها
│   │   ├── enums/             # انواع شمارشی
│   │   ├── repositories/      # قراردادهای مخزن
│   │   ├── types/             # تایپ‌های دامنه
│   │   └── valueObjects/      # اشیاء مقداری
│   ├── errors/                # مدیریت خطا
│   ├── features/              # ماژول‌های عملیاتی
│   │   ├── auth/              # احراز هویت
│   │   ├── audit/             # ممیزی
│   │   ├── automation/        # اتوماسیون
│   │   ├── dashboard/         # داشبورد
│   │   ├── decisions/         # تصمیمات
│   │   ├── files/             # فایل‌ها
│   │   ├── meeting-intelligence/ # هوش جلسات
│   │   ├── meetings/          # جلسات
│   │   ├── notifications/     # اعلان‌ها
│   │   ├── organization/      # سازمان
│   │   ├── performance/       # عملکرد
│   │   ├── reports/           # گزارش‌ها
│   │   ├── requests/          # درخواست‌ها
│   │   ├── search/            # جستجو
│   │   ├── tasks/             # وظایف
│   │   ├── users/             # کاربران
│   │   └── workflow/          # گردش‌کار
│   ├── hooks/                 # هوک‌های سفارشی
│   ├── layouts/               # لایوت‌های صفحه
│   ├── locales/               # محلی‌سازی (فارسی)
│   ├── repositories/          # پیاده‌سازی مخزن‌ها
│   ├── routes/                # پیکربندی مسیرها
│   ├── security/              # لایه امنیت
│   ├── services/              # سرویس‌ها
│   ├── stores/                # فروشگاه‌های وضعیت
│   ├── styles/                # فایل‌های CSS
│   │   ├── dashboard.css      # استایل‌های داشبورد
│   │   ├── components.css     # استایل‌های کامپوننت
│   │   ├── forms.css          # استایل‌های فرم
│   │   ├── layout.css         # استایل‌های لایوت
│   │   └── ...                # سایر استایل‌ها
│   ├── types/                 # تایپ‌های عمومی
│   └── utils/                 # توابع کمکی
├── scripts/                   # اسکریپت‌های اعتبارسنجی
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

## ۴. معماری نرم‌افزار

### لایه‌بندی معماری

```
┌─────────────────────────────────────────────┐
│              UI Layer (React)               │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐ │
│  │  Pages  │ │Components│ │   Widgets    │ │
│  └─────────┘ └──────────┘ └──────────────┘ │
├─────────────────────────────────────────────┤
│            Application Layer               │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐ │
│  │  Hooks  │ │  Stores  │ │   Services   │ │
│  └─────────┘ └──────────┘ └──────────────┘ │
├─────────────────────────────────────────────┤
│              Domain Layer                   │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐ │
│  │Entities │ │ ValueObj │ │  Repositories│ │
│  └─────────┘ └──────────┘ └──────────────┘ │
├─────────────────────────────────────────────┤
│            Infrastructure Layer             │
│  ┌─────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ Security│ │  Config  │ │    Utils     │ │
│  └─────────┘ └──────────┘ └──────────────┘ │
└─────────────────────────────────────────────┘
```

### الگوهای طراحی استفاده شده

| الگو | کاربرد |
|------|--------|
| **Feature-Based** | ساختار پوشه‌ها بر اساس ماژول‌ها |
| **Repository Pattern** | جداسازی دسترسی به داده |
| **Strategy Pattern** | سیاست‌های مختلف دسترسی |
| **Observer Pattern** | فروشگاه ری‌اکتیو (useSyncExternalStore) |
| **Composite Pattern** | کامپوننت‌های ترکیبی داشبورد |
| **Template Method** | state machine مشترک ویجت‌ها |
| **Factory Pattern** | ساخت view-modelها از داده خام |

---

## ۵. مدل داده و موجودیت‌ها

### موجودیت‌های اصلی (Domain Entities)

#### سازمان (Organization)

```typescript
interface User {
  id: string
  firstName: string
  lastName: string
  mobile: string
  username: string
  status: UserStatus // ACTIVE | INACTIVE
  createdAt: string
}

interface Department {
  id: string
  name: string
  description?: string
  status: UserStatus
}

interface Position {
  id: string
  name: string
  description?: string
  status: UserStatus
}

interface Role {
  id: string
  name: string
  code: RoleCode
}

interface Capability {
  id: string
  name: string
  code: string
  description?: string
}
```

#### گردش‌کار (Workflow)

```typescript
interface Meeting {
  id: string
  title: string
  description?: string
  createdBy: string
  startTime: string
  endTime?: string
  status: MeetingStatus
  createdAt: string
}

interface Task {
  id: string
  decisionId: string
  departmentId: string
  createdBy: string
  assignedTo: string
  title: string
  description: string
  priority: TaskPriority
  deadline: string
  status: TaskStatus
  createdAt: string
}

interface EmployeeReport {
  id: string
  employeeId: string
  departmentId: string
  taskId?: string
  title: string
  description: string
  status: ReportStatus
  attachments?: string[]
  managerComment?: string
  createdAt: string
}

interface Request {
  id: string
  type: RequestType
  title: string
  description: string
  createdBy: string
  departmentId: string
  priority: TaskPriority
  status: RequestStatus
  attachments?: string[]
  reviewReason?: string
  createdAt: string
}
```

### انواع شمارشی (Enums)

#### نقش‌ها

```typescript
RoleCode = {
  MAIN_MANAGER: 'MAIN_MANAGER'       // مدیرعامل
  DEPARTMENT_MANAGER: 'DEPARTMENT_MANAGER' // مدیر واحد
  SECRETARY: 'SECRETARY'             // دبیر جلسات
  EMPLOYEE: 'EMPLOYEE'              // کارشناس
}
```

#### وضعیت جلسه

```typescript
MeetingStatus = {
  DRAFT: 'DRAFT'                     // پیش‌نویس
  PENDING_APPROVAL: 'PENDING_APPROVAL' // در انتظار تأیید
  APPROVED: 'APPROVED'               // تأییدشده
  SCHEDULED: 'SCHEDULED'             // زمان‌بندی‌شده
  IN_PROGRESS: 'IN_PROGRESS'        // در حال برگزاری
  COMPLETED: 'COMPLETED'            // برگزارشده
  CANCELLED: 'CANCELLED'            // لغوشده
  REJECTED: 'REJECTED'              // ردشده
}
```

#### وضعیت وظیفه

```typescript
TaskStatus = {
  PENDING: 'PENDING'                // در انتظار
  IN_PROGRESS: 'IN_PROGRESS'       // در حال انجام
  WAITING_REVIEW: 'WAITING_REVIEW' // در انتظار بررسی
  COMPLETED: 'COMPLETED'           // انجام‌شده
  CANCELLED: 'CANCELLED'           // لغوشده
}
```

#### اولویت

```typescript
TaskPriority = {
  LOW: 'LOW'                       // کم
  MEDIUM: 'MEDIUM'                 // متوسط
  HIGH: 'HIGH'                     // زیاد
  CRITICAL: 'CRITICAL'             // بحرانی
}
```

#### نوع درخواست

```typescript
RequestType = {
  PURCHASE: 'PURCHASE'             // خرید
  REPAIR: 'REPAIR'                 // تعمیرات
  HR: 'HR'                         // منابع انسانی
  MEETING: 'MEETING'               // جلسه
  SUPPORT: 'SUPPORT'               // پشتیبانی
  PROCESS_CHANGE: 'PROCESS_CHANGE' // تغییر فرآیند
  CUSTOM: 'CUSTOM'                 // سفارشی
}
```

---

## ۶. سیستم احراز هویت و مجوزها

### سطوح دسترسی

```
┌─────────────────────────────────────────┐
│           نقش (Role)                    │
│  ┌─────────────────────────────────┐   │
│  │      قابلیت (Capability)        │   │
│  │  ┌─────────────────────────┐   │   │
│  │  │   مجوز (Permission)     │   │   │
│  │  └─────────────────────────┘   │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### قابلیت‌های سیستم (Capabilities)

| دسته‌بندی | قابلیت | توضیح |
|-----------|--------|--------|
| **سازمان** | `organization.manage` | مدیریت ساختار سازمانی |
| | `organization.view` | مشاهده ساختار سازمانی |
| | `position.manage` | مدیریت سمت‌ها |
| **کاربران** | `user.manage` | مدیریت کاربران |
| | `user.view` | مشاهده کاربران |
| | `role.manage` | مدیریت نقش‌ها |
| | `capability.manage` | مدیریت قابلیت‌ها |
| **جلسات** | `meeting.create` | ایجاد جلسه |
| | `meeting.manage` | مدیریت جلسات |
| | `meeting.view` | مشاهده جلسات |
| **صورتجلسه** | `minutes.create` | ایجاد صورتجلسه |
| | `minutes.edit` | ویرایش صورتجلسه |
| | `minutes.finalize` | نهایی‌سازی صورتجلسه |
| | `minutes.edit.final` | ویرایش صورتجلسه نهایی |
| **مصوبات** | `decision.create` | ایجاد مصوبه |
| | `decision.edit` | ویرایش مصوبه |
| | `decision.view` | مشاهده مصوبات |
| | `decision.edit.final` | ویرایش مصوبه نهایی |
| **وظایف** | `task.create` | ایجاد وظیفه |
| | `task.assign` | محول‌سازی وظیفه |
| | `task.review` | بررسی وظیفه |
| | `task.view.self` | مشاهده وظایف شخصی |
| | `task.update.self` | به‌روزرسانی وظیفه شخصی |
| | `task.view` | مشاهده همه وظایف |
| **گزارش‌ها** | `report.create` | ایجاد گزارش |
| | `report.view` | مشاهده گزارش‌ها |
| | `report.review` | بررسی گزارش |
| **درخواست‌ها** | `request.create` | ایجاد درخواست |
| | `request.review` | بررسی درخواست |
| | `request.view` | مشاهده درخواست‌ها |
| **سایر** | `notification.view` | مشاهده اعلان‌ها |
| | `file.manage` | مدیریت فایل‌ها |
| | `audit.view` | مشاهده ممیزی |
| | `dashboard.organization` | داشبورد سازمانی |
| | `dashboard.department` | داشبورد واحد |
| | `dashboard.self` | داشبورد شخصی |
| | `performance.organization` | عملکرد سازمانی |
| | `performance.department` | عملکرد واحد |
| | `performance.self` | عملکرد شخصی |

### تخصیص قابلیت‌ها بر اساس نقش

| قابلیت | مدیرعامل | مدیر واحد | دبیر جلسات | کارشناس |
|--------|:---------:|:---------:|:----------:|:-------:|
| `organization.manage` | ✅ | ❌ | ❌ | ❌ |
| `organization.view` | ✅ | ✅ | ✅ | ✅ |
| `meeting.create` | ✅ | ✅ | ✅ | ❌ |
| `meeting.view` | ✅ | ✅ | ✅ | ✅ |
| `task.create` | ✅ | ✅ | ❌ | ❌ |
| `task.assign` | ✅ | ✅ | ❌ | ❌ |
| `task.view` | ✅ | ✅ | ❌ | ❌ |
| `task.view.self` | ✅ | ✅ | ✅ | ✅ |
| `report.create` | ✅ | ✅ | ❌ | ✅ |
| `report.review` | ✅ | ✅ | ❌ | ❌ |
| `request.create` | ✅ | ✅ | ❌ | ✅ |
| `request.review` | ✅ | ✅ | ❌ | ❌ |
| `dashboard.organization` | ✅ | ❌ | ❌ | ❌ |
| `dashboard.department` | ❌ | ✅ | ❌ | ❌ |
| `dashboard.self` | ❌ | ❌ | ✅ | ✅ |
| `performance.organization` | ✅ | ❌ | ❌ | ❌ |
| `performance.department` | ❌ | ✅ | ❌ | ❌ |
| `performance.self` | ❌ | ❌ | ✅ | ✅ |

---

## ۷. نقش‌ها و داشبوردها

### نقش‌های سیستم

| نقش | کد | توضیح | داشبورد |
|-----|-----|--------|---------|
| **مدیرعامل** | `MAIN_MANAGER` | مدیریت کل سازمان | داشبورد مدیریت سازمان |
| **مدیر واحد** | `DEPARTMENT_MANAGER` | مدیریت یک واحد سازمانی | داشبورد مدیریت واحد |
| **دبیر جلسات** | `SECRETARY` | مدیریت جلسات و صورتجلسه‌ها | میز کار دبیرخانه جلسات |
| **کارشناس** | `EMPLOYEE` | انجام وظایف محول‌شده | فضای کاری من |

### داشبورد مدیرعامل (CEO)

```
┌─────────────────────────────────────────────────────┐
│ 📊 داشبورد مدیریت سازمان                           │
│ کیوان گستر • مدیرعامل • گروه صنعتی کیا گستر          │
│ نمای کلی عملکرد، جلسات و امور جاری سازمان            │
├─────────────────────────────────────────────────────┤
│ KPI Grid (۸ شاخص)                                  │
│ • کل کارکنان    • واحدهای سازمانی                   │
│ • جلسات فعال    • در انتظار تأیید                   │
│ • وظایف باز     • وظایف انجام‌شده                   │
│ • گزارش‌ها      • درخواست‌ها                        │
├─────────────────────────────────────────────────────┤
│ Band 1: نمودار روند فعالیت + وضعیت وظایف + عملکرد    │
│ Band 2: جدول واحدهای سازمانی + توزیع بار کاری       │
│ Band 3: رویدادهای پیش‌رو + فعالیت‌های سازمانی        │
│ Band 4: پنل شخصی‌سازی ظاهر                          │
└─────────────────────────────────────────────────────┘
```

**شاخص‌های کلیدی (KPI):**
- کل کارکنان سازمان
- تعداد واحدهای سازمانی
- جلسات فعال
- موارد در انتظار تأیید
- وظایف باز
- وظایف انجام‌شده
- گزارش‌های ثبت‌شده
- درخواست‌های در انتظار

**ویجت‌ها:**
- `ActivityTrendWidget` — نمودار روند فعالیت ۶ ماه اخیر
- `ChartWidget` — نمودار وضعیت وظایف (داینو)
- `DepartmentPerformanceWidget` — عملکرد واحدها
- `DepartmentOverviewWidget` — جدول مقایسه واحدها
- `ChartWidget` — توزیع بار کاری واحدها (میله‌ای)
- `UpcomingEvents` — رویدادهای پیش‌رو (جلسات + مهلت‌ها)
- `RecentActivityWidget` — فعالیت‌های اخیر سازمان
- `DashboardPersonalizationPanel` — شخصی‌سازی ظاهر

---

### داشبورد مدیر واحد (Department Manager)

```
┌─────────────────────────────────────────────────────┐
│ 📊 داشبورد مدیریت واحد                             │
│ محمدرضا کیانی • مدیر واحد • واحد فروش و بازاریابی    │
│ وضعیت تیم، جلسات و امور در جریان واحد شما           │
├─────────────────────────────────────────────────────┤
│ KPI Grid (۶ شاخص)                                  │
│ • وظایف واحد    • جلسات تیم                         │
│ • گزارش در انتظار بررسی • درخواست‌ها                 │
│ • وظایف دارای تأخیر • وظایف انجام‌شده               │
├─────────────────────────────────────────────────────┤
│ Band 1: جدول تیم + رویدادهای پیش‌رو + مهلت‌ها        │
│ Band 2: فعالیت‌های واحد + اعلان‌ها                   │
│ Band 3: پنل شخصی‌سازی                               │
└─────────────────────────────────────────────────────┘
```

**شاخص‌های کلیدی (KPI):**
- وظایف فعال واحد
- جلسات تیم
- گزارش‌های در انتظار بررسی
- درخواست‌های در انتظار
- وظایف دارای تأخیر
- وظایف انجام‌شده

**ویجت‌ها:**
- `TeamSummaryWidget` — جدول ظرفیت و بار کاری تیم
- `UpcomingEvents` — رویدادهای پیش‌رو واحد
- `DeadlinesWidget` — مهلت‌های نزدیک
- `RecentActivityWidget` — فعالیت‌های اخیر واحد
- `NotificationSummary` — اعلان‌های واحد
- `DashboardPersonalizationPanel` — شخصی‌سازی

---

### داشبورد دبیر جلسات (Secretary)

```
┌─────────────────────────────────────────────────────┐
│ 📋 میز کار دبیرخانه جلسات                           │
│ نگار شکوهی • دبیر جلسات • دفتر مدیرعامل            │
│ برنامه امروز، جلسات پیش‌رو و صورتجلسه‌های در جریان   │
├─────────────────────────────────────────────────────┤
│ KPI Grid (۴ شاخص)                                  │
│ • جلسات ایجادشده    • جلسات برگزارشده               │
│ • صورتجلسه‌های ناتمام • صورتجلسه‌های نهایی           │
├─────────────────────────────────────────────────────┤
│ Band 1: تقویم امروز + صورتجلسه‌ها + مصوبات          │
│ Band 2: مهلت‌های پیگیری + اعلان‌ها + فایل‌ها         │
│ Band 3: فعالیت‌های اخیر + پنل شخصی‌سازی             │
└─────────────────────────────────────────────────────┘
```

**شاخص‌های کلیدی (KPI):**
- جلسات ایجادشده
- جلسات برگزارشده
- صورتجلسه‌های ناتمام
- صورتجلسه‌های نهایی‌شده

**ویجت‌ها:**
- `CalendarPreviewWidget` — برنامه امروز و روزهای پیش‌رو
- `StatBreakdownWidget` — صورتجلسه‌ها (پیش‌نویس / نهایی‌شده)
- `StatBreakdownWidget` — مصوبات (در انتظار / در حال انجام / تکمیل‌شده)
- `DeadlinesWidget` — مهلت‌های پیگیری
- `NotificationSummary` — اعلان‌های اولویت‌دار
- `RecentFilesWidget` — فایل‌های اخیر
- `RecentActivityWidget` — فعالیت‌های اخیر من

---

### داشبورد کارشناس (Employee)

```
┌─────────────────────────────────────────────────────┐
│ 🏠 فضای کاری من                                     │
│ علی محمدی • کارشناس ارشد • واحد فنی و مهندسی        │
│ خلاصه وظایف، جلسات و درخواست‌های شما                │
├─────────────────────────────────────────────────────┤
│ KPI Grid (۶ شاخص)                                  │
│ • وظایف من    • انجام‌شده    • دارای تأخیر          │
│ • گزارش‌ها    • درخواست‌ها    • جلسات امروز          │
├─────────────────────────────────────────────────────┤
│ Band 1: وظایف من + جلسات من                         │
│ Band 2: گزارش‌ها + درخواست‌ها                        │
│ Band 3: مهلت‌ها + اعلان‌ها + فعالیت‌ها               │
│ Band 4: پنل شخصی‌سازی                               │
└─────────────────────────────────────────────────────┘
```

**شاخص‌های کلیدی (KPI):**
- وظایف من (فعال)
- وظایف انجام‌شده
- وظایف دارای تأخیر
- گزارش‌های در جریان
- درخواست‌های در انتظار
- جلسات امروز

**ویجت‌ها:**
- `StatBreakdownWidget` — وظایف من (در حال انجام / در انتظار / انجام‌شده / تأخیر)
- `CalendarPreviewWidget` — جلسات من
- `StatBreakdownWidget` — گزارش‌های من (پیش‌نویس / ارسال‌شده / بازگشتی / تأییدشده)
- `StatBreakdownWidget` — درخواست‌های من (در انتظار / تأییدشده / ردشده)
- `DeadlinesWidget` — مهلت‌های نزدیک
- `NotificationSummary` — اعلان‌های من
- `RecentActivityWidget` — فعالیت‌های من

---

## ۸. ماژول‌های عملیاتی

### ۸.۱. ماژول جلسات (Meetings)

**قابلیت‌ها:**
- ایجاد جلسه جدید (داخلی، واحد سازمانی، هیئت‌مدیره، پروژه‌ای، هفتگی)
- زمان‌بندی جلسه (حضوری، آنلاین، ترکیبی)
- مدیریت شرکت‌کنندگان
- تعریف دستورجلسه
- شروع و پایان جلسه
- ضبط صدای جلسه
- تهیه صورتجلسه
- ثبت مصوبات
- پیگیری مصوبات

**وضعیت‌های جلسه:**
```
پیش‌نویس → در انتظار تأیید → تأییدشده → زمان‌بندی‌شده → در حال برگزاری → برگزارشده
                                                      ↘ لغوشده
                                                      ↘ ردشده
```

**صفحه‌های اصلی:**
- `MeetingsListPage` — فهرست جلسات (جدول، کارت، فشرده)
- `MeetingWorkspacePage` — فضای کاری جلسه
- `LiveMeetingWorkspacePage` — جلسه زنده
- `SecretaryResolutionsPage` — مصوبات دبیرخانه

---

### ۸.۲. ماژول وظایف (Tasks)

**قابلیت‌ها:**
- ایجاد وظیفه جدید
- محول‌سازی به اعضا
- تعیین اولویت و مهلت
- پیگیری وضعیت
- گزارش‌دهی
- پیوست فایل
- نمایش تخته‌ای (Kanban)

**وضعیت‌های وظیفه:**
```
در انتظار → در حال انجام → در انتظار بررسی → انجام‌شده
                        ↘ دارای تأخیر
                        ↘ لغوشده
```

**صفحه‌های اصلی:**
- `TasksCollectionPage` — فهرست وظایف (جدول، تخته، کارت)
- جزئیات وظیفه
- وظایف واحد

---

### ۸.۳. ماژول گزارش‌ها (Reports)

**قابلیت‌ها:**
- ایجاد گزارش عملکردی
- ارسال برای بررسی
- زنجیره تأیید (کارشناس → مدیر واحد → مدیرعامل)
- بازگشت برای اصلاح
- نهایی‌سازی

**زنجیره تأیید:**
```
کارشناس → مدیر واحد → مدیرعامل → نهایی
              ↘ بازگشت برای اصلاح
```

**صفحه‌های اصلی:**
- `ReportsCollectionPage` — فهرست گزارش‌ها
- گزارش‌های کارکنان
- گزارش‌های واحدها

---

### ۸.۴. ماژول درخواست‌ها (Requests)

**قابلیت‌ها:**
- ایجاد درخواست (مرخصی، تجهیزات، بودجه، دسترسی، سایر)
- ارسال برای بررسی
- تأیید یا رد
- ارجاع به مدیر اصلی

**وضعیت‌های درخواست:**
```
پیش‌نویس → ارسال‌شده → در حال بررسی → تأییدشده
                                    ↘ ردشده
                                    ↘ ارجاع‌شده
```

**صفحه‌های اصلی:**
- `RequestsCollectionPage` — فهرست درخواست‌ها
- جزئیات درخواست
- بررسی درخواست

---

### ۸.۵. ماژول سازمان (Organization)

**قابلیت‌ها:**
- مشاهده ساختار سازمانی
- مدیریت واحدها
- مدیریت سمت‌ها
- چیدمان کاربران

**صفحه‌های اصلی:**
- `OrganizationPage` — ساختار سازمانی
- `DepartmentsPage` — واحدها
- `DepartmentDetailsPage` — جزئیات واحد
- `PositionsPage` — سمت‌ها
- `UsersOrganizationPage` — چیدمان کاربران

---

### ۸.۶. ماژول عملکرد (Performance)

**قابلیت‌ها:**
- ارزیابی عملکرد شخصی
- ارزیابی عملکرد واحد
- ارزیابی عملکرد سازمان
- نمودار عملکرد
- تاریخچه ارزیابی‌ها

**صفحه‌های اصلی:**
- `EmployeePerformanceDashboard` — عملکرد من
- `DepartmentPerformanceDashboard` — عملکرد واحد
- `OrganizationPerformanceDashboard` — عملکرد سازمان

---

### ۸.۷. ماژول اعلان‌ها (Notifications)

**قابلیت‌ها:**
- نمایش اعلان‌ها
- اولویت‌بندی (فوری، عادی، کم‌اهمیت)
- خوانده‌نشده / خوانده‌شده
- پیوند به منبع

**صفحه‌های اصلی:**
- `NotificationPage` — فهرست اعلان‌ها

---

### ۸.۸. ماژول فایل‌ها (Files)

**قابلیت‌ها:**
- بارگذاری فایل
- پیش‌نمایش فایل
- مدیریت پیوست‌ها
- پیوند به موجودیت

**صفحه‌های اصلی:**
- `FilesPage` — فهرست فایل‌ها
- `FileDetailsPage` — جزئیات فایل

---

### ۸.۹. ماژول ممیزی (Audit)

**قابلیت‌ها:**
- ثبت رویدادها
- تاریخچه تغییرات
- فعالیت کاربران
- لاگ امنیتی

**صفحه‌های اصلی:**
- `AuditPage` — رویدادهای سامانه
- `EntityHistoryPage` — تاریخچه
- `UserActivityPage` — فعالیت کاربر

---

### ۸.۱۰. ماژول جستجو (Search)

**قابلیت‌ها:**
- جستجوی سراسری (Ctrl+K)
- فیلتر بر اساس نوع
- فیلتر بر اساس تاریخ
- فیلتر بر اساس وضعیت
- تاریخچه جستجو
- پیشنهادات جستجو

---

### ۸.۱۱. ماژول گردش‌کار (Workflow)

**قابلیت‌ها:**
- مدیریت گردش‌کار
- تاریخچه تغییرات
- تأیید و ارجاع

**صفحه‌های اصلی:**
- `WorkflowPage` — گردش‌کار

---

### ۸.۱۲. ماژول اتوماسیون (Automation)

**قابلیت‌ها:**
- تعریف قوانین خودکار
- ماشه‌ها (Trigger): ایجاد وظیفه، تأخیر وظیفه، ارسال گزارش، تکمیل جلسه
- اقدامات خودکار
- تاریخچه اجرا

---

### ۸.۱۳. ماژول هوش جلسات (Meeting Intelligence)

**قابلیت‌ها:**
- تحلیل جلسات
- پیشرفت تصمیمات
- آمار جلسات
- تایم‌لاین جلسات

---

## ۹. سیستم UI و کامپوننت‌ها

### کتابخانه UI اختصاصی

تمام کامپوننت‌های UI از صفر و بدون وابستگی خارجی پیاده‌سازی شده‌اند.

#### کامپوننت‌های پایه

| کامپوننت | توضیح |
|----------|--------|
| `Button` | دکمه با انواع variant و size |
| `Card` | کارت محتوا |
| `Avatar` | آواتار کاربر |
| `Badge` | نشان وضعیت |
| `Chip` | تراشه |
| `Divider` | خط جداکننده |
| `Icon` | آیکون SVG |
| `Surface` | سطح پس‌زمینه |
| `Typography` | تایپوگرافی |
| `StatusBadge` | نشان وضعیت رنگی |
| `Tag` | برچسب |

#### کامپوننت‌های فرم

| کامپوننت | توضیح |
|----------|--------|
| `TextField` | فیلد متنی |
| `TextArea` | فیلد متنی چندخطی |
| `Select` | لیست کشویی |
| `MultiSelect` | انتخاب چندگانه |
| `DatePicker` | انتخابگر تاریخ |
| `TimePicker` | انتخابگر زمان |
| `FileInput` | بارگذاری فایل |
| `Checkbox` | چک‌باکس |
| `Switch` | کلید |
| `OTPField` | فیلد کد یکبارمصرف |

#### کامپوننت‌های نمایش داده

| کامپوننت | توضیح |
|----------|--------|
| `Table` | جدول داده |
| `DataGrid` | شبکه داده |
| `DataCard` | کارت داده |
| `PropertyList` | لیست ویژگی‌ها |
| `DescriptionList` | لیست توصیفات |
| `Statistic` | آمار |
| `Progress` | نوار پیشرفت |
| `LinearProgress` | نوار پیشرفت خطی |
| `Timeline` | تایم‌لاین |
| `FilterBar` | نوار فیلتر |
| `Toolbar` | نوار ابزار |
| `Pagination` | صفحه‌بندی |
| `EmptyState` | وضعیت خالی |
| `ErrorState` | وضعیت خطا |
| `LoadingState` | وضعیت بارگذاری |

#### کامپوننت‌های پوشش (Overlay)

| کامپوننت | توضیح |
|----------|--------|
| `Modal` | پنجره модال |
| `Drawer` | کشویی |
| `Toast` | اعلان شناور |
| `Menu` | منو |
| `Popover` | Popover |
| `ConfirmDialog` | دیالوگ تأیید |

#### کامپوننت‌های نمودار

| کامپوننت | توضیح |
|----------|--------|
| `BarChart` | نمودار میله‌ای |
| `LineChart` | نمودار خطی |
| `DonutChart` | نمودار دایره‌ای |
| `MultiSeriesLineChart` | نمودار چندسری |

#### کامپوننت‌های داشبورد

| کامپوننت | توضیح |
|----------|--------|
| `DashboardWidget` | ویجت پایه |
| `DashboardHeader` | هدر داشبورد |
| `DashboardGrid` | شبکه داشبورد |
| `DashboardComposedLayout` | لایوت ترکیبی |
| `DashboardAutoLayout` | لایوت خودکار |
| `KPIGrid` | شبکه KPI |
| `KPIWidget` | ویجت KPI |
| `QuickActions` | اقدامات سریع |
| `UpcomingEvents` | رویدادهای پیش‌رو |
| `ActivityFeed` | فید فعالیت |
| `DashboardPersonalizationPanel` | پنل شخصی‌سازی |

#### کامپوننت‌های ویژه داشبورد

| کامپوننت | توضیح |
|----------|--------|
| `RecentActivityWidget` | فعالیت‌های اخیر |
| `DepartmentOverviewWidget` | نمای واحدها |
| `DepartmentPerformanceWidget` | عملکرد واحدها |
| `TeamSummaryWidget` | خلاصه تیم |
| `CalendarPreviewWidget` | پیش‌نمایش تقویم |
| `DeadlinesWidget` | مهلت‌ها |
| `RecentFilesWidget` | فایل‌های اخیر |
| `PendingApprovalsWidget` | تأییدهای منتظر |
| `StatBreakdownWidget` | تفکیک آمار |
| `ChartWidget` | ویجت نمودار |
| `NotificationSummary` | خلاصه اعلان‌ها |

---

## ۱۰. گردش‌کار و فرآیندها

### گردش‌کار جلسه

```
                    ┌──────────────┐
                    │  ایجاد جلسه  │
                    │   (پیش‌نویس) │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │  زمان‌بندی   │
                    │   جلسه      │
                    └──────┬───────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
       ┌──────▼──────┐ ┌──▼──┐ ┌──────▼──────┐
       │  در انتظار   │ │ تأیید│ │   ردشده    │
       │  تأیید      │ │ شده │ │            │
       └──────┬──────┘ └──┬──┘ └────────────┘
              │           │
              │     ┌─────▼──────┐
              │     │  شروع جلسه │
              │     │ (در حال    │
              │     │  برگزاری)  │
              │     └─────┬──────┘
              │           │
              │     ┌─────▼──────┐
              │     │  پایان     │
              │     │  جلسه      │
              │     │ (برگزارشده)│
              │     └─────┬──────┘
              │           │
              │     ┌─────▼──────┐
              │     │ تهیه       │
              │     │ صورتجلسه   │
              │     └─────┬──────┘
              │           │
              │     ┌─────▼──────┐
              │     │ ثبت        │
              │     │ مصوبات     │
              │     └─────┬──────┘
              │           │
              │     ┌─────▼──────┐
              │     │ ایجاد      │
              │     │ وظایف      │
              │     └────────────┘
              │
       ┌──────▼──────┐
       │  لغو جلسه   │
       └─────────────┘
```

### گردش‌کار گزارش

```
┌──────────────┐
│  ایجاد گزارش │
│  (پیش‌نویس)  │
└──────┬───────┘
       │
┌──────▼───────┐
│   ارسال      │
└──────┬───────┘
       │
┌──────▼───────┐     ┌──────────────┐
│ بررسی مدیر   │────▶│ بازگشت برای  │
│   واحد      │◀────│   اصلاح      │
└──────┬───────┘     └──────────────┘
       │
┌──────▼───────┐     ┌──────────────┐
│ بررسی        │────▶│ بازگشت برای  │
│ مدیرعامل    │◀────│   اصلاح      │
└──────┬───────┘     └──────────────┘
       │
┌──────▼───────┐
│  نهایی‌سازی   │
│  (تأییدشده)  │
└──────────────┘
```

### گردش‌کار درخواست

```
┌──────────────┐
│  ایجاد       │
│  درخواست     │
└──────┬───────┘
       │
┌──────▼───────┐
│   ارسال      │
└──────┬───────┘
       │
┌──────▼───────┐
│  بررسی       │
│  مدیر واحد  │
└──────┬───────┘
       │
  ┌────┼────┐
  │    │    │
┌─▼─┐┌─▼─┐┌─▼──────┐
│تأیید││ رد││ ارجاع  │
└─┬─┘└─┬─┘│ مدیر   │
  │    │  │ اصلی   │
  │    │  └────────┘
  ▼    ▼
```

---

## ۱۱. امنیت

### سیاست‌های امنیتی

| سیاست | توضیح |
|-------|--------|
| **RBAC** | کنترل دسترسی مبتنی بر نقش |
| **Capability-Based** | کنترل دسترسی مبتنی بر قابلیت |
| **Scope-Based** | کنترل دسترسی مبتنی بر محدوده |
| **Guard Pattern** | محافظت مسیرها و عملیات |

### ساختار امنیتی

```
┌─────────────────────────────────────────┐
│            Security Layer               │
│  ┌─────────────┐  ┌─────────────────┐  │
│  │    can()    │  │    guard()      │  │
│  │  بررسی      │  │  محافظت        │  │
│  │  مجوز      │  │  مسیر/عملیات   │  │
│  └─────────────┘  └─────────────────┘  │
│  ┌─────────────┐  ┌─────────────────┐  │
│  │   scope()   │  │  PermissionGuard│  │
│  │  محدوده     │  │  محافظت کامپوننت│  │
│  └─────────────┘  └─────────────────┘  │
└─────────────────────────────────────────┘
```

### فایل‌های امنیتی

| فایل | توضیح |
|------|--------|
| `security/can.ts` | تابع بررسی مجوز |
| `security/guards.ts` | محافظ‌های مسیر |
| `security/scope.ts` | مدیریت محدوده |
| `security/accessControl.ts` | کنترل دسترسی |
| `security/permissions.ts` | تعریف قابلیت‌ها |
| `core/authorization/PermissionGuard.tsx` | محافظت کامپوننت |
| `core/authorization/ProtectedRoute.tsx` | محافظت مسیر |

---

## ۱۲. تنظیمات و اجرا

### پیش‌نیازها

- **Node.js** >= 18
- **npm** >= 9

### نصب وابستگی‌ها

```bash
npm install
```

### اجرای توسعه

```bash
npm run dev
```

### بیلد برای تولید

```bash
npm run build
```

### پیش‌نمایش بیلد

```bash
npm run preview
```

### بررسی کیفیت کد

```bash
npm run lint
```

### اجرای تست‌ها

```bash
npm test
```

### اسکریپت‌های اعتبارسنجی

| اسکریپت | توضیح |
|----------|--------|
| `verify:security` | اعتبارسنجی امنیت |
| `verify:auth` | اعتبارسنجی احراز هویت |
| `verify:workflows` | اعتبارسنجی گردش‌کار |
| `verify:phase9` | اعتبارسنجی فاز ۹ |
| `verify:ui` | اعتبارسنجی کامپوننت‌های UI |
| `verify:phase11` | اعتبارسنجی فاز ۱۱ |
| `verify:calendar` | اعتبارسنجی تقویم |
| `verify:targeted` | اعتبارسنجی جریان‌های هدفمند |

---

## ۱۳. پیشنهادات بهبود UX

### ۱۳.۱. بهبود داشبوردها

#### اصول کلی

1. **نوار فوریت بالای صفحه** — هر داشبورد با «چی نیاز به اقدام من داره؟» شروع بشه
2. **KPI کمتر** — ۸ شاخص → ۴ شاخص (فقط مهم‌ترین‌ها)
3. **ادغام بخش‌های مشابه** — مثلاً گزارش+درخواست → یه بخش
4. **تقویم فقط امروز+فردا** — نه ۶ رویداد آینده
5. **فعالیت‌ها کمتر** — ۸ مورد → ۳-۴ مورد
6. **پنل شخصی‌سازی ساده** — فقط چک‌باکس نمایش/مخفی کردن
7. **لینک‌های سریع** — هر بخش لینک «مشاهده همه» داشته باشه

#### بهبود داشبورد مدیرعامل

| فعلی | پیشنهاد |
|------|---------|
| ۸ KPI | ۴ KPI فوری |
| ۲ نمودار + ۱ جدول | ۱ نوار پیشرفت ساده |
| ۶ رویداد آینده | فقط امروز + فردا |
| ۸ فعالیت | ۳-۴ فعالیت |

#### بهبود داشبورد مدیر واحد

| فعلی | پیشنهاد |
|------|---------|
| ۶ KPI | ۴ KPI فوری |
| جدول تیم کامل | جدول ساده‌تر |
| رویدادها + مهلت‌ها جداگانه | ادغام در یک لیست |
| اعلان‌ها جداگانه | فقط اقدامات منتظر |

#### بهبود داشبورد دبیر جلسات

| فعلی | پیشنهاد |
|------|---------|
| ۴ KPI | ۴ KPI فوری |
| صورتجلسه + مصوبات + فایل جداگانه | ادغام در «پیگیری جلسات» |
| ۸ فعالیت | ۲-۳ فعالیت |

#### بهبود داشبورد کارشناس

| فعلی | پیشنهاد |
|------|---------|
| ۶ KPI | ۴ KPI فوری |
| StatBreakdown پیچیده | آمار ساده + لیست اقدامات |
| گزارش + درخواست جداگانه | ادغام در یک بخش |
| ۶ فعالیت | ۳ فعالیت |

### ۱۳.۲. بهبود ناوبری

| # | پیشنهاد | توضیح |
|---|---------|--------|
| ۱ | **سایدبار ساده‌تر** | گروه‌بندی منطقی‌تر، نام‌های ساده‌تر |
| ۲ | **صفحه پیگیری کارها** | یه صفحه مرکزی برای همه کارهای کاربر |
| ۳ | **جستجوی سریع** | Ctrl+K همه جا کار کنه |
| ۴ | **اعلان هوشمند** | فوری/مهم/اطلاع‌رسانی |

### ۱۳.۳. بهبود فرم‌ها

| # | پیشنهاد | توضیح |
|---|---------|--------|
| ۱ | **فرم‌های مرحله‌ای** | Wizard به جای فرم طولانی |
| ۲ | **فیلدهای ضروری مشخص** | ستاره‌دار کردن فیلدهای الزامی |
| ۳ | **فیلدهای اختیاری مخفی** | پشت «گزینه‌های بیشتر» |

---

## ۱۴. فهرست فایل‌ها

### فایل‌های اصلی

| فایل | توضیح |
|------|--------|
| `package.json` | پیکربندی پروژه |
| `vite.config.ts` | پیکربندی Vite |
| `tsconfig.json` | پیکربندی TypeScript |
| `eslint.config.js` | پیکربندی ESLint |

### فایل‌های هسته

| فایل | توضیح |
|------|--------|
| `src/main.tsx` | نقطه ورود |
| `src/RootApp.tsx` | ریشه اپلیکیشن |
| `src/app/AppProvider.tsx` | ارائه‌دهنده اپلیکیشن |

### فایل‌های مسیریابی

| فایل | توضیح |
|------|--------|
| `src/routes/routeConfig.ts` | پیکربندی مسیرها |
| `src/routes/publicRoutes.ts` | مسیرهای عمومی |
| `src/app/navigation/navigationConfig.ts` | پیکربندی ناوبری |

### فایل‌های امنیت

| فایل | توضیح |
|------|--------|
| `src/security/can.ts` | تابع بررسی مجوز |
| `src/security/guards.ts` | محافظ‌ها |
| `src/security/scope.ts` | مدیریت محدوده |
| `src/security/accessControl.ts` | کنترل دسترسی |
| `src/security/permissions.ts` | تعریف قابلیت‌ها |

### فایل‌های دامنه

| فایل | توضیح |
|------|--------|
| `src/domain/entities/organization.ts` | موجودیت‌های سازمانی |
| `src/domain/entities/workflow.ts` | موجودیت‌های گردش‌کار |
| `src/domain/enums/index.ts` | انواع شمارشی |

### فایل‌های داده نمونه

| فایل | توضیح |
|------|--------|
| `src/demo/demoDataset.ts` | مجموعه داده اصلی |
| `src/demo/demoOrganization.ts` | داده‌های سازمانی |
| `src/demo/demoMeetings.ts` | داده‌های جلسات |
| `src/demo/demoWorkItems.ts` | داده‌های کاری |
| `src/demo/demoDashboard.ts` | داده‌های داشبورد |
| `src/demo/demoStore.ts` | فروشگاه ری‌اکتیو |
| `src/demo/demoTypes.ts` | تایپ‌های داده نمونه |
| `src/demo/demoViews.ts` | ویوهای ارائه‌ای |
| `src/demo/demoMeetingViews.ts` | ویوهای جلسات |

### فایل‌های کامپوننت داشبورد

| فایل | توضیح |
|------|--------|
| `src/components/dashboard/DashboardShell.tsx` | پوسته داشبورد |
| `src/components/dashboard/DashboardWidgets.tsx` | ویجت‌های داشبورد |
| `src/components/dashboard/useDashboardPersonalization.ts` | هوک شخصی‌سازی |
| `src/features/dashboard/pages/RoleDashboards.tsx` | داشبوردهای نقش‌محور |
| `src/features/dashboard/dashboardPersonalizationConfig.ts` | پیکربندی شخصی‌سازی |

### فایل‌های استایل

| فایل | توضیح |
|------|--------|
| `src/styles/dashboard.css` | استایل‌های داشبورد |
| `src/styles/components.css` | استایل‌های کامپوننت |
| `src/styles/forms.css` | استایل‌های فرم |
| `src/styles/layout.css` | استایل‌های لایوت |
| `src/styles/responsive.css` | استایل‌های واکنش‌گرا |
| `src/styles/rtl.css` | پشتیبانی RTL |
| `src/styles/typography.css` | تایپوگرافی |
| `src/styles/variables.css` | متغیرهای CSS |

---

## 📌 نکات فنی

### تصمیمات معماری

1. **بدون وابستگی UI** — تمام کامپوننت‌ها از صفر نوشته شده‌اند
2. **DDD** — جداسازی لایه دامنه از زیرساخت
3. **Feature-Based** — ساختار پوشه‌ها بر اساس ماژول‌ها
4. **Reactive Store** — استفاده از `useSyncExternalStore`
5. **Personalization** — ذخیره تنظیمات در localStorage
6. **Auto-Sync** — همگام‌سازی خودکار وضعیت جلسات با ساعت سیستم

### معیارهای کیفیت

- **TypeScript strict mode**
- **ESLint با قوانین اختصاصی**
- **اسکریپت‌های اعتبارسنجی خودکار**
- **دسترسی‌پذیری (ARIA)**
- **پشتیبانی RTL**
- **بیلد بهینه با Vite**

---

> **نسخه:** 0.0.0
> **تاریخ آخرین به‌روزرسانی:** ۱۴۰۴
> **تعداد فایل‌ها:** ۵۳۳+
> **تعداد کامپوننت‌ها:** ۱۲۷+
> **تعداد ماژول‌ها:** ۱۳+
