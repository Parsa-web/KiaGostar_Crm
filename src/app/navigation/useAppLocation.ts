import {useCallback,useEffect,useState} from 'react'
export function useAppLocation(){const [path,setPath]=useState(()=>window.location.pathname);useEffect(()=>{const update=()=>setPath(window.location.pathname);window.addEventListener('popstate',update);window.addEventListener('app:navigate',update);return()=>{window.removeEventListener('popstate',update);window.removeEventListener('app:navigate',update)}},[]);const navigate=useCallback((target:string,{replace=false}:{replace?:boolean}={})=>{if(target===window.location.pathname)return;window.history[replace?'replaceState':'pushState'](null,'',target);window.dispatchEvent(new Event('app:navigate'));window.scrollTo({top:0,behavior:'auto'})},[]);return{path,navigate}}
