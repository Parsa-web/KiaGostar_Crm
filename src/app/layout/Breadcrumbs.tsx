import {Icon} from '../../components/ui/Icon';import {AppLink} from '../navigation/AppLink';import type {BreadcrumbItem} from './breadcrumbUtils'
export function Breadcrumbs({items}:{items:readonly BreadcrumbItem[]}){if(!items.length)return null;return <nav className="breadcrumbs" aria-label="مسیر صفحه"><ol className="breadcrumb-list">{items.map((item,index)=><li key={`${item.label}-${index}`}>{index>0&&<Icon name="chevron" size={14} className="directional-icon"/>}{item.path?<AppLink href={item.path}>{item.label}</AppLink>:<span aria-current="page">{item.label}</span>}</li>)}</ol></nav>}
