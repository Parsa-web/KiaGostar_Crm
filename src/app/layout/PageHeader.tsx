import type {ReactNode} from 'react';import {Breadcrumbs} from './Breadcrumbs';import type {BreadcrumbItem} from './breadcrumbUtils'
export interface PageHeaderProps{title:ReactNode;description?:ReactNode;breadcrumbs?:readonly BreadcrumbItem[];status?:ReactNode;metadata?:ReactNode;primaryAction?:ReactNode;secondaryActions?:ReactNode;tabs?:ReactNode;loading?:boolean}
export function PageHeader({title,description,breadcrumbs=[],status,metadata,primaryAction,secondaryActions,tabs,loading}:PageHeaderProps){if(loading)return <header className="page-header skeleton"><div className="skeleton-line"/><div className="skeleton-line"/></header>;return <header className="page-header"><Breadcrumbs items={breadcrumbs}/><div className="page-header__row"><div className="page-header__copy"><div className="page-header__title-row"><h1 id="page-title" tabIndex={-1}>{title}</h1>{status}</div>{description&&<p>{description}</p>}{metadata&&<div className="page-header__metadata">{metadata}</div>}</div>{(primaryAction||secondaryActions)&&<div className="page-header__actions">{secondaryActions}{primaryAction}</div>}</div>{tabs&&<div className="page-header__tabs">{tabs}</div>}</header>}
