import type {AutomationEvent,AutomationRule,Condition} from '../types'
const evaluate=(event:AutomationEvent,condition:Condition)=>{const value=condition.field.split('.').reduce<unknown>((current,key)=>current&&typeof current==='object'?(current as Record<string,unknown>)[key]:undefined,event);if(condition.operator==='EQUALS')return value===condition.value;if(condition.operator==='IN')return Array.isArray(condition.value)&&condition.value.includes(value);if(condition.operator==='BEFORE')return Date.parse(String(value))<Date.parse(String(condition.value));if(condition.operator==='AFTER')return Date.parse(String(value))>Date.parse(String(condition.value));return Date.parse(String(value))<Date.now()}
export const evaluateRule=(rule:AutomationRule,event:AutomationEvent)=>rule.enabled&&rule.trigger===event.type&&rule.conditions.every((condition)=>evaluate(event,condition))
