import type {AssistantContext,Suggestion} from '../types'
export class AssistantService{analyzeContext(context:AssistantContext){return{overdue:context.tasks.filter((task)=>task.deadline&&Date.parse(task.deadline)<Date.now()&&task.status!=='COMPLETED'),pending:context.pendingActions.length}}getSuggestions(context:AssistantContext):Suggestion[]{const analysis=this.analyzeContext(context);return[...analysis.overdue.map((task)=>({id:`task-${task.id}`,type:'TASK' as const,message:`وظیفه «${task.title}» از موعد گذشته است.`,priority:'HIGH' as const})),...(analysis.pending?[{id:'workflow-pending',type:'WORKFLOW' as const,message:`${analysis.pending} اقدام در انتظار بررسی است.`,priority:'MEDIUM' as const}]:[])]}generateInsight(context:AssistantContext){const suggestions=this.getSuggestions(context);return{summary:suggestions.length?`${suggestions.length} پیشنهاد قابل بررسی وجود دارد.`:'اقدام فوری پیشنهاد نمی‌شود.',suggestions}}}
