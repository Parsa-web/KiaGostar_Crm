import { AlertCard, ChartCard, DashboardGrid, DashboardSection, MetricCard } from '../components'; import type { DashboardData } from '../types'
const Dashboard = ({ data }: { data: DashboardData }) => <main><header><h1>{data.summary.title}</h1></header><DashboardSection title="شاخص‌ها"><DashboardGrid>{data.metrics.map((metric) => <MetricCard key={metric.title} metric={metric} />)}</DashboardGrid></DashboardSection><DashboardSection title="تحلیل"><DashboardGrid>{data.charts.map((chart) => <ChartCard key={chart.id} chart={chart} />)}</DashboardGrid></DashboardSection>{data.alerts.length > 0 && <DashboardSection title="نیازمند توجه"><DashboardGrid>{data.alerts.map((alert) => <AlertCard key={alert.id} alert={alert} />)}</DashboardGrid></DashboardSection>}</main>
export const MainManagerDashboard = Dashboard; export const DepartmentManagerDashboard = Dashboard; export const EmployeeDashboard = Dashboard; export const SecretaryDashboard = Dashboard
