import type { Task } from '../../../domain/entities'
export const TaskDetails = ({ task, children }: { task: Task; children?: React.ReactNode }) => <article><h2>{task.title}</h2><p>{task.description}</p><dl><dt>تصمیم مبنا</dt><dd>{task.decisionId}</dd><dt>مسئول</dt><dd>{task.assignedTo}</dd></dl>{children}</article>
export const TaskCreateForm = ({ visible, onSubmit }: { visible: boolean; onSubmit?: () => void }) => visible ? <form onSubmit={(event) => { event.preventDefault(); onSubmit?.() }}><h2>ایجاد وظیفه از تصمیم</h2><button type="submit">ثبت وظیفه</button></form> : null
export const TaskReportForm = ({ onSubmit }: { onSubmit?: (description: string) => void }) => <form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit?.(String(form.get('description') ?? '')) }}><textarea name="description" aria-label="شرح گزارش" required /><button type="submit">ارسال گزارش</button></form>
export const TaskReviewPanel = ({ onApprove, onReject, onCancel }: { onApprove?: () => void; onReject?: () => void; onCancel?: () => void }) => <div className="button-row"><button type="button" onClick={onApprove}>تأیید</button><button type="button" onClick={onReject}>بازگشت برای اصلاح</button><button type="button" onClick={onCancel}>لغو</button></div>
