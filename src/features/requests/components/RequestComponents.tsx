import type { Request } from '../../../domain/entities'
export const RequestForm = ({ onSubmit }: { onSubmit?: (description: string) => void }) => <form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit?.(String(form.get('description') ?? '')) }}><textarea name="description" aria-label="شرح درخواست" required /><button type="submit">ثبت درخواست</button></form>
export const RequestDetails = ({ request }: { request: Request }) => <article><h2>{request.title}</h2><p>{request.description}</p><dl><dt>وضعیت</dt><dd>{request.status}</dd><dt>اولویت</dt><dd>{request.priority}</dd></dl></article>
export const RequestReviewPanel = ({ onApprove, onReject, onEscalate }: { onApprove?: () => void; onReject?: () => void; onEscalate?: () => void }) => <div className="button-row"><button type="button" onClick={onApprove}>تأیید</button><button type="button" onClick={onReject}>رد</button><button type="button" onClick={onEscalate}>ارجاع به مدیر اصلی</button></div>
