import type { DepartmentReport, EmployeeReport } from '../../../domain/entities'
import { EmptyState } from '../../../components'
export const EmployeeReportForm = ({ onSubmit }: { onSubmit?: (description: string) => void }) => <form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit?.(String(form.get('description') ?? '')) }}><textarea name="description" aria-label="شرح گزارش کار" required /><button type="submit">ذخیره گزارش</button></form>
export const ReportReviewPanel = ({ onApprove, onCorrection }: { onApprove?: () => void; onCorrection?: () => void }) => <div className="button-row"><button type="button" onClick={onApprove}>تأیید گزارش</button><button type="button" onClick={onCorrection}>درخواست اصلاح</button></div>
export const DepartmentReportBuilder = ({ onSubmit }: { onSubmit?: (summary: string) => void }) => <form onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); onSubmit?.(String(form.get('summary') ?? '')) }}><textarea name="summary" aria-label="خلاصه گزارش واحد" required /><button type="submit">ساخت گزارش واحد</button></form>
export const ReportsList = ({ employeeReports = [], departmentReports = [] }: { employeeReports?: readonly EmployeeReport[]; departmentReports?: readonly DepartmentReport[] }) => employeeReports.length || departmentReports.length ? <ul className="entity-list">{employeeReports.map((item) => <li key={item.id}>{item.title} — {item.status}</li>)}{departmentReports.map((item) => <li key={item.id}>{item.period} — {item.status}</li>)}</ul> : <EmptyState label="گزارشی وجود ندارد." />
