import {forwardRef,type HTMLAttributes,type ReactNode} from 'react';import {cx} from './utils'
export interface CardProps extends HTMLAttributes<HTMLElement>{variant?:'default'|'outlined'|'elevated'|'interactive';media?:ReactNode;header?:ReactNode;footer?:ReactNode;actions?:ReactNode;children:ReactNode}
export const Card=forwardRef<HTMLElement,CardProps>(function Card({variant='default',media,header,footer,actions,children,className,tabIndex,...props},ref){const interactive=variant==='interactive';return <article ref={ref} className={cx('ui-card',`ui-card--${variant}`,className)} tabIndex={interactive?(tabIndex??0):tabIndex} {...props}>{media&&<div className="ui-card__media">{media}</div>}{header&&<header className="ui-card__header">{header}</header>}<div className="ui-card__body">{children}</div>{(footer||actions)&&<footer className="ui-card__footer">{footer}<div className="ui-card__actions">{actions}</div></footer>}</article>})
