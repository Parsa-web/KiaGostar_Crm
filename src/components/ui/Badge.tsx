import type {ReactNode} from 'react';import {statusLabels} from '../../app/presentation/labels';import {cx} from './utils'
export type BadgeVariant='neutral'|'success'|'warning'|'danger'|'info';export interface BadgeProps{children?:ReactNode;variant?:BadgeVariant;status?:string;count?:number;max?:number;dot?:boolean;compact?:boolean;className?:string}
export function Badge({children,variant='neutral',status,count,max=99,dot=false,compact=false,className}:BadgeProps){const content=count===undefined?(status?statusLabels[status]??status:children):(count>max?`${max.toLocaleString('fa-IR')}+`:count.toLocaleString('fa-IR'));return <span className={cx('ui-badge',`ui-badge--${variant}`,dot&&'ui-badge--dot',compact&&'ui-badge--compact',className)} data-status={status}>{!dot&&content}<span className="visually-hidden">{dot?children:null}</span></span>}
