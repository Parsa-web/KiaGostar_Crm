import type { Pagination as PaginationModel } from '../../types'
export const Pagination = ({ value, onChange }: { value: PaginationModel; onChange(page: number): void }) => { const pages = Math.max(1, Math.ceil(value.total / value.limit)); return <nav className="pagination" aria-label="صفحه‌بندی"><button type="button" disabled={value.page <= 1} onClick={() => onChange(value.page - 1)}>قبلی</button><span>صفحه {value.page} از {pages}</span><button type="button" disabled={value.page >= pages} onClick={() => onChange(value.page + 1)}>بعدی</button></nav> }
export const PageSizeSelector = ({ value, onChange }: { value: number; onChange(size: number): void }) => <label>تعداد در صفحه<select value={value} onChange={(event) => onChange(Number(event.target.value))}>{[10, 20, 50].map((size) => <option key={size} value={size}>{size}</option>)}</select></label>
export const DataTablePagination = ({ value, onPageChange, onSizeChange }: { value: PaginationModel; onPageChange(page: number): void; onSizeChange(size: number): void }) => <div className="table-pagination"><PageSizeSelector value={value.limit} onChange={onSizeChange} /><Pagination value={value} onChange={onPageChange} /></div>
